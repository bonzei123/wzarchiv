version: '3.8'

services:
  zeitung-downloader:
    image: ghcr.io/bonzei123/wzarchiv:latest
    container_name: zeitung-downloader
    restart: always
    ports:
      - "6789:5000"
    environment:
      - RUNNING_IN_DOCKER=true
      - TZ=Europe/Berlin
    # HIER ÜBERSCHREIBEN WIR DEN BEFEHL VOM DOCKERFILE
    command: gunicorn -w 4 -b 0.0.0.0:5000 --timeout 120 app:app
    volumes:
      - ./downloads:/app/downloads
    env_file:
      - .env